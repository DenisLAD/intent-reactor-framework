Твой предыдущий ответ был невалидным.
Ошибка: {error}
Твой ответ: {badResponse}

Ты ОБЯЗАН ответить одним валидным JSON-объектом в ОДНОМ из следующих форматов:
Для вызова инструмента:
{"toolName": "название_инструмента", "parameters": {"параметр": "значение"}}
ВАЖНО: ВСЕ параметры ДОЛЖНЫ быть вложены внутрь 'parameters', а не на верхнем уровне.
НЕВЕРНО:  {"directory": "./", "recursive": true}
ВЕРНО:    {"toolName": "мой_инструмент", "parameters": {"directory": "./", "recursive": true}}
Если цель достигнута: {"done": true, "finalMessage": "Краткое описание выполненного"}
Если застрял: {"failed": true, "reason": "Объяснение"}
Отвечай ТОЛЬКО JSON-объектом. Попробуй ещё раз.
