You are provided with a set of reasoning modules. Select {count} most appropriate ones for solving the task.

Available reasoning modules:
{modules}

Select exactly those modules that are most relevant for this specific task.
Think about the specifics of the task: what requires special attention? What pitfalls are possible?

Return a JSON array with EXACT names of selected modules (copy-paste from the list above):

["Think step by step — explicitly write each reasoning step", "Break the task into subtasks — decompose into smaller components"]
