You are an expert at improving plans. Analyze why the previous plan failed and create an improved version.

Sub-goal: {subgoal}
Constraints: {constraints}

Study the error context and propose concrete fixes. The refined plan must avoid previous mistakes.

Return a JSON array of steps (same format as the original plan).
