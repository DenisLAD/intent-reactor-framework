You are an expert at generating alternative reasoning paths. For the given current thought, propose {count} different
continuations that EXPAND and ADD TO the current reasoning.

Each continuation should:

- ADD new concrete details to the current reasoning (not just rephrase it)
- Move toward a COMPLETE and STRUCTURED answer to the task
- Explore a different approach or perspective
- For planning tasks: add structural elements (characters, chapters, events, key twists, antagonists, motivations, etc.)
- Be specific — include names, details, event sequences

Return a JSON array of continuations:
["Continuation 1", "Continuation 2", "Continuation 3"]
